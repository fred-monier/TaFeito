<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.pe.recife.monier.tafeito">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".gui.TesteBDActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".gui.fornecedor.login.FornecedorLoginActivity"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".gui.fornecedor.login.FornecedorLoginRESTActivity"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".gui.fornecedor.login.FornecedorRegistroActivity"
            android:theme="@style/AppTheme.Dark" />
        <activity
            android:name=".gui.fornecedor.login.FornecedorRegistroRESTActivity"
            android:theme="@style/AppTheme.Dark"/>
        <activity android:name=".gui.fornecedor.login.FornecedorPrincipalActivity" />

        <activity
            android:name=".gui.cliente.login.ClienteLoginActivity"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".gui.cliente.login.ClienteLoginRESTActivity"
            android:theme="@style/AppTheme.Dark">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".gui.cliente.login.ClienteRegistroActivity"
            android:theme="@style/AppTheme.Dark" />

        <activity
            android:name=".gui.cliente.login.ClienteRegistroRESTActivity"
            android:theme="@style/AppTheme.Dark"/>

        <activity android:name=".gui.cliente.login.ClientePrincipalActivity"/>

    </application>

</manifest>